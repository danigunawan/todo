import api from './api'
import auth from './auth'
import filter from './filter'
import todo from './todo'

export default {
  api: api.types,
  auth: auth.types,
  filter: filter.types,
  todo: todo.types
}
